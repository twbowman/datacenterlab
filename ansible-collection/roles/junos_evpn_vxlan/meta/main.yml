---
galaxy_info:
  author: Production Network Testing Lab
  description: Juniper Junos EVPN/VXLAN configuration role
  company: Network Automation Lab

  license: MIT

  min_ansible_version: "2.9"

  platforms:
    - name: Junos
      versions:
        - all

  galaxy_tags:
    - networking
    - juniper
    - junos
    - evpn
    - vxlan
    - datacenter

dependencies:
  - role: junos_bgp
    when: evpn_vxlan.bgp_evpn.enabled | default(false)
